#!/usr/bin/env node
import * as cdk from 'aws-cdk-lib';

// Temporary placeholder - will be replaced in Step 7
const app = new cdk.App();

// eslint-disable-next-line no-new
new cdk.Stack(app, 'PlaceholderStack');
